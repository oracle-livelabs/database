# SEER Telecomms conversion handoff

The target repository is `/Users/mkowalik/Documents/GitHub/oracle-livelabs/database/livestack-workshop-telecommunications`. All 116 source files remain unchanged, verified against the initial hashes and inventory.

## Completed

- Preserved 11 lessons, 40 tasks, 60 SQL blocks, both manifests and two notebooks with 51 paragraphs.
- Implemented 15 telecom tables, 18 foreign keys and 11,491 explicit sample rows, plus 192 generated 384-dimensional embeddings. Subscribers, plans, access sites, service orders, connection quantities, diagnostics, activation evidence and prepaid airtime transfers align across the loader and labs.
- Executed the main loader statements in Database Actions sections and batches. Assertions, table counts, views, JSON duality, vectors, spatial objects and the property graph passed. The SQLcl CONNECT/password entrypoint remains untested.
- Ran relational/JSON/vector/spatial SQL, both Graph Studio notebooks, OML training/scoring and AutoML, Select AI and the corrected agent example. The final agent run succeeded with one SQL-tool call in 22.159 seconds. Earlier failed attempts and their corrections are documented.
- Preserved eight persona banners visually with updated telecom captions. Added the Powtoon/Redwood-style opening story illustration and core ERD, with a native SVG companion. Raster caption editing does not guarantee pixel identity outside the text areas.
- Placed 48 authentic database screenshots and nine authentic application screenshots beside their instructions. The workshop contains 77 image files; all 69 image placements on the nine revised SQL lesson pages loaded in the local renderer.
- Passed 45 static check groups; 37 SQL blocks parsed with SQLGlot and 23 Oracle-specific blocks received manual review. Live results are recorded separately. No prior-domain business residue was found in readable files, filenames, notebook/JSON/SVG content or reviewed/OCR-scanned raster images.

## Platform terms and limits

LiveLabs Reservation Information/My Reservations, Oracle Property Graph and the Graph Studio built-in moviestream tag are platform terminology. The supplied application's dataset and local llama3.2 runtime differ from the workshop database. OML's perfect reported score comes from sample labels calculated from the same month’s inputs and is not a forecast-quality claim. Agent objects and a disabled validation-only profile are retained for review.

## Remaining green-button phase

No Terraform plan/apply or LiveLabs green-button provisioning was attempted. Validate generated setup-template ordering, SPATIAL_AUTHOR setup, model download and provider-region availability, wallet/SQLcl arguments and the full loader entrypoint, infrastructure outputs, and sandbox/tenancy launch behavior. Manual database success does not establish provisioning success.

## Delivery evidence

The complete workshop ZIP and standalone stack ZIP exclude only macOS metadata. Packaging verifies CRC integrity and compares every archive entry with the latest target file. `archive-integrity.json` records archive sizes, SHA-256 values, entry counts and source protection. The standalone SQL is identical to the main loader in both archives.

See `manual-database-validation.md`, `schema-contract.md`, `screenshots.md`, `domain-audit.json` and `archive-integrity.json` beside this report. The workshop ZIP also contains the full validation directory and selected live transcripts.
