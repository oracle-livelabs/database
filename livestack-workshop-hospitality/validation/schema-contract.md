# Hospitality schema contract

This is the Phase 1 implementation contract for the forthcoming loader. It specifies required objects; it does not claim that they already exist in a running database. The [machine-readable contract](schema-contract.json) supports static checks.

## Naming and relationships

Keep the workshop owner `LLUSER`, the provider profile `GENAI`, and the shared embedding model `ADMIN.ALL_MINILM_L12_V2`. Use unquoted uppercase snake-case object names, singular `_ID` keys, `_V` reporting views, and `_DV` duality views. SQL examples use lowercase for readability. New constraints should use `PK_`, `FK_`, `UQ_`, and `CK_` prefixes; indexes use `IX_`. Generate IDs by default or use explicit fixture IDs; the lab inserts its own reservation and night-line IDs.

The core chain is HOTEL_PROPERTIES → STAY_OFFERS; GUESTS → RESERVATIONS → RESERVATION_NIGHTS → STAY_OFFERS. Reservations also reference HOTEL_PROPERTIES. Do not duplicate hotel properties as a separate routing facility entity. Individual room assignment, restaurants, employees, and payment settlement are outside these exercises.

## Objects and columns

### HOTEL_PROPERTIES

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `PROPERTY_ID` | NUMBER PK |
| `PROPERTY_NAME` | VARCHAR2(120) |
| `CITY` | VARCHAR2(100) |
| `STATE_PROVINCE` | VARCHAR2(100) |
| `LATITUDE` | NUMBER |
| `LONGITUDE` | NUMBER |
| `LOCATION` | SDO_GEOMETRY |
| `ROOM_CAPACITY` | NUMBER |
| `OCCUPIED_ROOMS_PCT` | NUMBER |
| `IS_ACTIVE` | NUMBER(1) |



### STAY_OFFERS

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `OFFER_ID` | NUMBER PK |
| `PROPERTY_ID` | NUMBER FK |
| `OFFER_NAME` | VARCHAR2(160) |
| `ROOM_TYPE` | VARCHAR2(80) |
| `RATE_PLAN` | VARCHAR2(80) |
| `CATEGORY` | VARCHAR2(100) |
| `SUBCATEGORY` | VARCHAR2(100) |
| `NIGHTLY_RATE` | NUMBER(12,2) |
| `IS_ACTIVE` | NUMBER(1) |

One offer is a room type plus rate plan at one property; prices and room revenue use USD for the fixture. OFFER_EMBEDDING VECTOR(384) is added only in Lab 3.

### GUESTS

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `GUEST_ID` | NUMBER PK |
| `FIRST_NAME` | VARCHAR2(80) |
| `LAST_NAME` | VARCHAR2(80) |
| `EMAIL` | VARCHAR2(200) |
| `LOYALTY_TIER` | VARCHAR2(30) |
| `LOCATION` | SDO_GEOMETRY |
| `PREFERENCES` | JSON |

LOCATION is a requested arrival/relocation point. Use synthetic example.com contacts and optional preferences, never real guest data.

### RESERVATIONS

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `RESERVATION_ID` | NUMBER PK |
| `GUEST_ID` | NUMBER FK |
| `PROPERTY_ID` | NUMBER FK |
| `CHECK_IN` | DATE |
| `CHECK_OUT` | DATE |
| `RESERVATION_STATUS` | VARCHAR2(30) |
| `RESERVATION_TOTAL` | NUMBER(12,2) |
| `SERVICE_FEE` | NUMBER(12,2) |
| `DEMAND_SCORE` | NUMBER |
| `CREATED_AT` | TIMESTAMP DEFAULT SYSTIMESTAMP |

One room per reservation, checkout exclusive; statuses pending/confirmed/checked_in/checked_out/cancelled/no_show. Nullable demand score; default creation timestamp. ID columns GENERATED BY DEFAULT or explicit IDs so the lab can insert its reserved IDs.

### RESERVATION_NIGHTS

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `NIGHT_LINE_ID` | NUMBER PK |
| `RESERVATION_ID` | NUMBER FK |
| `OFFER_ID` | NUMBER FK |
| `ROOM_NIGHTS` | NUMBER |
| `NIGHTLY_RATE` | NUMBER(12,2) |
| `LINE_TOTAL` | NUMBER(12,2) generated |

Aggregated nightly-charge line. LINE_TOTAL = ROOM_NIGHTS * NIGHTLY_RATE. Line offer must belong to the reservation property. Sum ROOM_NIGHTS equals CHECK_OUT-CHECK_IN for the one-room fixture. Distinct nightly rates may use separate lines.

### GUEST_POSTS

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `POST_ID` | NUMBER PK |
| `GUEST_ID` | NUMBER FK nullable |
| `POST_TEXT` | VARCHAR2(4000) |
| `SENTIMENT` | NUMBER |
| `LIKES` | NUMBER |
| `SHARES` | NUMBER |
| `VIEWS` | NUMBER |
| `VIRALITY` | NUMBER |
| `SEVERITY_SCORE` | NUMBER |
| `AFFECTED_RESERVATIONS` | NUMBER |
| `SERVICE_CASES_OPENED` | NUMBER |

Synthetic guest service observations and social activity; post metrics support the same OML exercise as the reference.

### POST_OFFER_MENTIONS

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `POST_ID` | NUMBER PK FK |
| `OFFER_ID` | NUMBER PK FK |

Composite PK avoids duplicate post/offer joins.

### OFFER_EMBEDDINGS

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `OFFER_ID` | NUMBER PK FK |
| `EMBEDDING` | VECTOR(384) |

Preloaded with ADMIN.ALL_MINILM_L12_V2 before Lab 1. Lab 3 separately creates STAY_OFFERS.OFFER_EMBEDDING with the same model and text recipe.

### DEMAND_REGIONS

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `REGION_ID` | NUMBER PK |
| `REGION_NAME` | VARCHAR2(120) UNIQUE |
| `DEMAND_INDEX` | NUMBER |
| `BOUNDARY` | SDO_GEOMETRY |



### BOOKING_ENTITIES

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `ENTITY_ID` | NUMBER PK |
| `ENTITY_KEY` | VARCHAR2(100) UNIQUE |
| `DISPLAY_NAME` | VARCHAR2(160) |
| `ENTITY_TYPE` | VARCHAR2(30) |
| `RISK_SCORE` | NUMBER |
| `RISK_LEVEL` | VARCHAR2(20) |
| `CHANNEL` | VARCHAR2(30) |
| `TOTAL_AMOUNT` | NUMBER(12,2) |
| `EVENT_COUNT` | NUMBER |
| `IS_CONFIRMED_ABUSE` | NUMBER(1) |
| `RESERVATION_ID` | NUMBER FK nullable |
| `PROPERTY_ID` | NUMBER FK nullable |

Types reservation/device/ip_address/phone/email/payment_token/property. Reservation and property vertices link to their business rows. TOTAL_AMOUNT is associated booking value, not money transferred among entities.

### BOOKING_RELATIONSHIPS

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `RELATIONSHIP_ID` | NUMBER PK |
| `FROM_ENTITY` | NUMBER FK |
| `TO_ENTITY` | NUMBER FK |
| `RELATIONSHIP_TYPE` | VARCHAR2(60) |
| `STRENGTH` | NUMBER |
| `EVENT_COUNT` | NUMBER |
| `TOTAL_AMOUNT` | NUMBER(12,2) |

Directed evidence links; use USES_DEVICE, USES_TOKEN, USES_PHONE, USES_IP, AT_PROPERTY, and IDENTIFIES_RESERVATION. Reverse evidence links must be explicit when directed multi-hop paths require them.

### BOOKING_CASES

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `CASE_ID` | NUMBER PK |
| `CASE_REF` | VARCHAR2(80) UNIQUE |
| `CASE_TYPE` | VARCHAR2(40) |
| `STATUS` | VARCHAR2(30) |
| `RISK_SCORE` | NUMBER |
| `LOSS_AMOUNT` | NUMBER(12,2) |
| `EVENT_COUNT` | NUMBER |



### BOOKING_CASE_ENTITIES

table; created by `loader`.

| Column | Type / role |
| --- | --- |
| `CASE_ENTITY_ID` | NUMBER PK |
| `CASE_ID` | NUMBER FK |
| `ENTITY_ID` | NUMBER FK |
| `ROLE` | VARCHAR2(40) |
| `EVIDENCE_SCORE` | NUMBER |



### STAY_OFFERS_V

view; created by `loader`.

| Column | Type / role |
| --- | --- |
| `OFFER_ID` | NUMBER |
| `OFFER_NAME` | VARCHAR2(160) |
| `PROPERTY_ID` | NUMBER |
| `OFFER_CATEGORY` | VARCHAR2(100) |

Projects STAY_OFFERS.CATEGORY as OFFER_CATEGORY; one row per offer.

### HOTEL_PROPERTIES_V

view; created by `loader`.

| Column | Type / role |
| --- | --- |
| `PROPERTY_ID` | NUMBER |
| `PROPERTY_NAME` | VARCHAR2(120) |
| `CITY` | VARCHAR2(100) |
| `STATE_PROVINCE` | VARCHAR2(100) |

One row per HOTEL_PROPERTIES property.

### SERVICE_ALERTS_V

view; created by `loader`.

| Column | Type / role |
| --- | --- |
| `ALERT_ID` | NUMBER |
| `SEVERITY_SCORE` | NUMBER |
| `AFFECTED_RESERVATIONS` | NUMBER |
| `SERVICE_CASES_OPENED` | NUMBER |

Projects GUEST_POSTS.POST_ID as ALERT_ID. Counts describe each alert; sum across alerts is not a unique reservation count.

### OML_STAY_DEMAND_TRAINING_V

view; created by `loader`.

| Column | Type / role |
| --- | --- |
| `OFFER_ID` | NUMBER |
| `CATEGORY` | VARCHAR2(100) |
| `NIGHTLY_RATE` | NUMBER |
| `TOTAL_POSTS` | NUMBER |
| `AVG_SENTIMENT` | NUMBER |
| `TOTAL_LIKES` | NUMBER |
| `TOTAL_SHARES` | NUMBER |
| `TOTAL_VIEWS` | NUMBER |
| `AVG_VIRALITY` | NUMBER |
| `VIRAL_POSTS` | NUMBER |
| `RISING_POSTS` | NUMBER |
| `ROOM_NIGHTS_BOOKED` | NUMBER |
| `REVENUE` | NUMBER |
| `SURGE_LABEL` | VARCHAR2(10) |

One row per active offer. Preaggregate posts separately from reservation lines to prevent join fanout. Booked room revenue sums LINE_TOTAL for confirmed/checked_in/checked_out only. Seed both SURGE and STABLE labels. Record synthetic label rules, aggregation window, and thresholds in the Phase 2 loader; they are not validated forecasts.

### RESERVATIONS_DV

duality_view; created by `loader`.

| Column | Type / role |
| --- | --- |
| `DATA` | JSON |

Initial loader view has the exact Lab 2 JSON shape but WITH UPDATE on root and child. Lab 2 enables WITH INSERT UPDATE. JSON keys: _id, guestId, propertyId, checkIn, checkOut, status, total, serviceFee, demandScore, createdAt, items[{nightLineId,offerId,roomNights,nightlyRate}].

### THOMAS_APP_DATA

table; created by `lab2`.

| Column | Type / role |
| --- | --- |
| `RESERVATION_ID` | NUMBER PK |
| `APP_DATA` | JSON |



### THOMAS_RESERVATION_DOCS

json_collection; created by `lab2`.

| Column | Type / role |
| --- | --- |
| `DATA` | JSON |

Application-owned snapshot, WITH ETAG; does not synchronize automatically with base reservation tables.

### OTTO_STAY_DEMAND_SETTINGS

table; created by `lab6`.

| Column | Type / role |
| --- | --- |
| `SETTING_NAME` | VARCHAR2(30) |
| `SETTING_VALUE` | VARCHAR2(4000) |



### OTTO_STAY_DEMAND_SCORING_DATA

table; created by `lab6`.

| Column | Type / role |
| --- | --- |
| `OFFER_ID` | NUMBER |
| `CATEGORY` | VARCHAR2(100) |
| `NIGHTLY_RATE` | NUMBER |
| `TOTAL_POSTS` | NUMBER |
| `AVG_SENTIMENT` | NUMBER |
| `TOTAL_LIKES` | NUMBER |
| `TOTAL_SHARES` | NUMBER |
| `TOTAL_VIEWS` | NUMBER |
| `AVG_VIRALITY` | NUMBER |
| `VIRAL_POSTS` | NUMBER |
| `RISING_POSTS` | NUMBER |
| `ROOM_NIGHTS_BOOKED` | NUMBER |
| `REVENUE` | NUMBER |

Synthetic perturbation of training rows; no SURGE_LABEL and not an independent holdout.

### LOYALTY_MEMBERS

table; created by `optional_loader`.

| Column | Type / role |
| --- | --- |
| `MEMBER_ID` | NUMBER PK |
| `GUEST_ID` | NUMBER FK |
| `NAME` | VARCHAR2(160) |

Optional PGX vertices; MEMBER_ID identifies a hotel loyalty member.

### LOYALTY_POINT_TRANSFERS

table; created by `optional_loader`.

| Column | Type / role |
| --- | --- |
| `TRANSFER_ID` | NUMBER PK |
| `FROM_MEMBER_ID` | NUMBER FK |
| `TO_MEMBER_ID` | NUMBER FK |
| `POINTS` | NUMBER |
| `DESCRIPTION` | VARCHAR2(200) |

Directed transfer of hotel loyalty points. PGQL edge label TRANSFERS.

## Foreign keys

| Child | Parent |
| --- | --- |
| `STAY_OFFERS.PROPERTY_ID` | `HOTEL_PROPERTIES.PROPERTY_ID` |
| `RESERVATIONS.GUEST_ID` | `GUESTS.GUEST_ID` |
| `RESERVATIONS.PROPERTY_ID` | `HOTEL_PROPERTIES.PROPERTY_ID` |
| `RESERVATION_NIGHTS.RESERVATION_ID` | `RESERVATIONS.RESERVATION_ID` |
| `RESERVATION_NIGHTS.OFFER_ID` | `STAY_OFFERS.OFFER_ID` |
| `GUEST_POSTS.GUEST_ID` | `GUESTS.GUEST_ID` |
| `POST_OFFER_MENTIONS.POST_ID` | `GUEST_POSTS.POST_ID` |
| `POST_OFFER_MENTIONS.OFFER_ID` | `STAY_OFFERS.OFFER_ID` |
| `OFFER_EMBEDDINGS.OFFER_ID` | `STAY_OFFERS.OFFER_ID` |
| `BOOKING_ENTITIES.RESERVATION_ID` | `RESERVATIONS.RESERVATION_ID` |
| `BOOKING_ENTITIES.PROPERTY_ID` | `HOTEL_PROPERTIES.PROPERTY_ID` |
| `BOOKING_RELATIONSHIPS.FROM_ENTITY` | `BOOKING_ENTITIES.ENTITY_ID` |
| `BOOKING_RELATIONSHIPS.TO_ENTITY` | `BOOKING_ENTITIES.ENTITY_ID` |
| `BOOKING_CASE_ENTITIES.CASE_ID` | `BOOKING_CASES.CASE_ID` |
| `BOOKING_CASE_ENTITIES.ENTITY_ID` | `BOOKING_ENTITIES.ENTITY_ID` |
| `LOYALTY_MEMBERS.GUEST_ID` | `GUESTS.GUEST_ID` |
| `LOYALTY_POINT_TRANSFERS.FROM_MEMBER_ID` | `LOYALTY_MEMBERS.MEMBER_ID` |
| `LOYALTY_POINT_TRANSFERS.TO_MEMBER_ID` | `LOYALTY_MEMBERS.MEMBER_ID` |

## Required fixture and constraints

- Property 1, guest 1, and stay offer 1 must exist. Offer 1 belongs to property 1 and costs 125.00 per night in the JSON example. Reserve reservation 900001 and night-line 990001 for Lab 2; do not prepopulate either ID. The example covers 22–24 September 2026, one room, two room nights, line total 250.00, service fee zero.
- Enforce check-out after check-in, positive integral room nights, nonnegative prices/fees, valid statuses, severity and demand indices in 0–100, occupancy in 0–100, and active flags in 0/1. Enforce cross-table property/date consistency through loader validation or suitable database logic; do not invent an invalid cross-table CHECK constraint. For the fixture, RESERVATION_TOTAL equals room-line totals plus SERVICE_FEE.
- Include at least 12 active offers across standard rooms, accessible rooms, family suites, and extended stays. Include step-free-access wording in offer names/category/subcategory because those three fields form the embedding text. Do not promise exact similarity rankings.
- Include posts with severity ≥80, linked to offers, and positive affected-reservation/service-case counts. Avoid duplicate POST_OFFER_MENTIONS rows. Training data needs both SURGE and STABLE, nonconstant features, and enough examples for the chosen training split. The synthetic labels demonstrate APIs, not future demand accuracy.
- Geometry uses SRID 8307 (WGS84), longitude first, with latitude/longitude columns matching property points. Seed properties 1, 3, and 16; guest arrival points inside both visitor regions; active hotels near them; valid region polygons. New York Visitor Region and Chicago Visitor Region use synthetic demand indices 91 and 78. Names such as Seer Edison Hotel and Seer Joliet Hotel are fictional. Exact distances are unmeasured.
- BOOKING_ABUSE_NETWORK is precreated before Lab 4 using the appendix definition. Seed RSV-8841, RSV-5077, RSV-1190 as reservation vertices tied to corresponding reservation rows; connect them to DEV-fp-91a7. Include TOKEN-REUSED-017, IP-198.51.100.44, and a synthetic phone. At least one shared-identifier pair needs a risk score ≥70. Include directed paths of one through four hops and a case edge. No graph score is proof of misconduct; tokens contain no card details.
- Optional LOYALTY_GRAPH is a PGQL graph, loaded with `session.read_graph_by_name(..., "PG_PGQL")`. It is not interchangeable with the SQL property graph. Seed members 534, 597, 934, 387, and 406 and explicit points-transfer cycles of four and five hops, a six-hop path, and multiple paths between 934 and 387. Each member maps to a guest. Graph Studio import must preserve the `LOYALTY_MEMBERS(id)` vertex identity expected by PGX. Verify the actual identity format after provisioning.
- Before Lab 1, preload OFFER_EMBEDDINGS and RESERVATIONS_DV. Leave STAY_OFFERS.OFFER_EMBEDDING absent until Lab 3. Before Lab 6, prepare OML_STAY_DEMAND_TRAINING_V with all 13 feature columns and SURGE_LABEL. Lab-created objects must initially be absent; use a fresh workshop schema for a full rerun.
- GENAI needs a configured provider, credential reference, network access, and appropriate package privileges. Its object list is STAY_OFFERS, RESERVATIONS, RESERVATION_NIGHTS, GUESTS. The object list guides generation; grants and row-level policies enforce access. Do not put credentials in the repository.
- The source's sandbox and tenancy launch pages share the same flow. Phase 2 must reconcile the actual Terraform outputs and deployment instructions for each route.

## Technical references

Oracle's [duality update documentation](https://docs.oracle.com/en/database/oracle/oracle-database/26/jsnvu/updating-documents-data-duality-views.html) describes document and field updates. The [agent package reference](https://docs.oracle.com/en-us/iaas/autonomous-database-serverless/doc/dbms-cloud-ai-agent-package.html) describes the built-in SQL tool. The [PGX loading guide](https://docs.oracle.com/en/cloud/paas/autonomous-database/csgru/load-graphs-memory-programmatically.html) distinguishes graph loading into memory. These references informed static review; they do not replace a live run.
